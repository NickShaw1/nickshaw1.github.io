import{a as e}from"./markdown-hfXSdh9Q.js";import{t}from"./KBAside-B9ZG6R1A.js";import{t as n}from"./KBNote-CaRhdL4n.js";import{n as r,r as i,t as a}from"./KBHeading-DROUfOXR.js";var o=e();function s(){return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(i,{children:`Integration testing verifies that separate components or systems work correctly together. Where unit tests confirm that individual pieces of logic are correct in isolation, integration tests confirm that those pieces behave correctly when connected to their real dependencies. The distinction matters because many defects only surface at the boundary between components, not within any single component in isolation.`}),(0,o.jsx)(a,{id:`what-integration-testing-covers`,children:`What integration testing covers`}),(0,o.jsx)(i,{children:`An integration test exercises at least two real components interacting with each other. Common examples include a service calling a real database, an API handler processing a real HTTP request, or a module consuming a real message queue. The test verifies that the interaction produces the expected outcome: that data is stored correctly, that the response is structured as expected, that a message triggers the right downstream action.`}),(0,o.jsx)(i,{children:`Integration tests tolerate more complexity and slower execution than unit tests because they are testing things that unit tests cannot: the correctness of database queries, the handling of serialisation and deserialisation, the behaviour of authentication middleware and the accuracy of third-party API integrations. These concerns are invisible to unit tests that replace dependencies with stubs.`}),(0,o.jsx)(a,{id:`types-of-integration-test`,children:`Types of integration test`}),(0,o.jsx)(r,{children:`Component integration tests`}),(0,o.jsx)(i,{children:`These tests verify that a specific component integrates correctly with one or more of its immediate dependencies. A typical example is testing that a data access layer correctly reads and writes to a database. The test uses a real database connection, executes the code under test and verifies that the state of the database matches expectations after the operation completes. Other external dependencies may still be stubbed.`}),(0,o.jsx)(r,{children:`API integration tests`}),(0,o.jsx)(i,{children:`These tests drive the application through its external API, verifying that the entire request-handling stack works correctly end to end within the service. They exercise routing, request parsing, business logic, database interaction and response formatting as an integrated whole. They provide broader coverage than component tests but run more slowly and are harder to isolate when they fail.`}),(0,o.jsx)(r,{children:`Contract tests`}),(0,o.jsx)(i,{children:`Contract tests are a distinct category that verifies the interface between a service and its consumers. Rather than testing the full behaviour of an integration, they verify that a provider service continues to fulfil the expectations documented by each consumer. They are covered in more detail in the contract testing article.`}),(0,o.jsx)(a,{id:`scope-decisions`,children:`Scope and boundary decisions`}),(0,o.jsx)(i,{children:`A key decision in integration testing is how much of the system to include in each test. Broader scope means more realistic coverage but slower, harder-to-diagnose tests. Narrower scope means faster, more focused tests but more reliance on stubs that may not accurately represent the real dependency.`}),(0,o.jsx)(i,{children:`A common approach is to draw the boundary at the service level for most integration tests, using a real database and real internal components but stubbing external services, queues and third-party APIs. This provides meaningful integration coverage for the areas the team controls while keeping the tests fast enough to run in a CI pipeline.`}),(0,o.jsx)(t,{label:`Test containers`,variant:`blue`,children:`Tools such as Testcontainers allow integration tests to spin up real database or message broker instances in Docker containers as part of the test run. This gives each test run a clean, isolated environment without requiring a shared test database. The containers start before the tests and are discarded afterwards, making the tests self-contained and avoiding state leakage between runs.`}),(0,o.jsx)(a,{id:`integration-vs-unit`,children:`Integration tests vs unit tests`}),(0,o.jsx)(i,{children:`Integration tests and unit tests are not alternatives; they test different things and both are necessary in a mature test suite. Unit tests verify logic; integration tests verify connectivity. A unit test can confirm that a function correctly builds a database query string. Only an integration test can confirm that the query actually works against the database and returns the expected results.`}),(0,o.jsx)(i,{children:`The relative proportion of each depends on the nature of the application. Systems with complex business logic benefit more from extensive unit test coverage. Systems that are primarily about data flow and external integrations benefit more from integration test coverage. Most real applications benefit from both, with the testing pyramid providing a reasonable default: more unit tests than integration tests because unit tests are faster and cheaper to maintain.`}),(0,o.jsx)(n,{variant:`green`,children:`Integration tests that share state between runs are a common source of unreliable test results. If one test inserts a record into a database and a subsequent test expects an empty table, the order of execution determines whether the second test passes. Ensuring each test either cleans up after itself or runs against a fresh state is foundational to a reliable integration test suite.`})]})}export{s as default};