import{a as e}from"./markdown-hfXSdh9Q.js";import{t}from"./KBAside-B9ZG6R1A.js";import{t as n}from"./KBNote-CaRhdL4n.js";import{r,t as i}from"./KBHeading-DROUfOXR.js";var a=e();function o(){return(0,a.jsxs)(a.Fragment,{children:[(0,a.jsx)(r,{children:`Snapshot testing records the rendered output of a component or function at a point in time and fails if that output changes in subsequent test runs. The first time a snapshot test runs, it serialises the output and saves it as a reference file. On subsequent runs, it serialises the output again and compares it to the stored snapshot. Any difference causes the test to fail, prompting a developer to review whether the change was intentional and update the snapshot if it was.`}),(0,a.jsx)(i,{id:`what-snapshot-testing-is`,children:`What snapshot testing is`}),(0,a.jsx)(r,{children:`Snapshot tests are most commonly used with UI components, where the snapshot captures the rendered HTML or component tree. A snapshot of a button component records its structure, class names, attributes and text content. If a future code change alters any of these, the snapshot comparison fails and the developer is alerted.`}),(0,a.jsx)(r,{children:`Snapshot testing can also be applied to non-UI outputs: the shape of an API response, the output of a serialisation function or the structure of a generated configuration file. Any deterministic output that can be serialised to text is a candidate for a snapshot test.`}),(0,a.jsx)(i,{id:`when-snapshots-help`,children:`When snapshots help`}),(0,a.jsx)(r,{children:`Snapshots are useful as a lightweight safety net for outputs that are complex and difficult to assert against explicitly. A component that renders a complex tree of elements with many attributes would require a significant number of individual assertions to verify exhaustively. A snapshot test captures all of that structure in a single test and detects any change to any part of it.`}),(0,a.jsx)(r,{children:`They are also useful during refactoring. When restructuring implementation code without intending to change visible output, snapshot tests provide a quick confirmation that the rendered result is unchanged. If a snapshot fails during a refactor, it is an immediate signal that the change affected the output in some way that requires review.`}),(0,a.jsx)(t,{label:`Inline snapshots`,variant:`gold`,children:`Some testing frameworks support inline snapshots, where the serialised output is stored directly in the test file rather than in a separate snapshot file. This makes the expected value visible alongside the test code, which aids reviewability. Inline snapshots work well for small outputs. For large or frequently changing outputs, a separate snapshot file is easier to manage.`}),(0,a.jsx)(i,{id:`the-staleness-problem`,children:`The staleness problem`}),(0,a.jsx)(r,{children:`The most significant weakness of snapshot testing is the ease with which snapshots become stale. When a snapshot test fails, the developer must decide whether the change is intentional or a regression. If the change is intentional, the developer updates the snapshot. Over time, in a codebase where snapshots are updated routinely, developers begin updating them reflexively rather than reviewing them carefully.`}),(0,a.jsx)(r,{children:`A snapshot that is always updated when it fails provides no safety net. It documents what the output was, but it does not enforce what the output should be. The discipline of reviewing snapshot diffs before updating them is essential to maintaining their value, and it is a discipline that requires active attention.`}),(0,a.jsx)(n,{variant:`blue`,children:`Large snapshots are a warning sign. A snapshot that is hundreds of lines long is difficult to review meaningfully when it changes. Where possible, test components at a granular level, keeping snapshots small enough that a change in the diff is immediately interpretable.`}),(0,a.jsx)(i,{id:`snapshot-vs-visual`,children:`Snapshot vs visual regression`}),(0,a.jsx)(r,{children:`Snapshot testing and visual regression testing are often confused because both detect changes to rendered output. The difference is in what they capture. Snapshot tests capture the logical structure of rendered output as serialised text: the component tree, HTML markup or function output. Visual regression tests capture the pixel-level appearance of rendered UI in a real browser.`}),(0,a.jsx)(r,{children:`A snapshot test will detect a structural change, such as a class name being removed or an element being replaced with a different type, but it will not detect a visual change that results from a CSS rule change applied to an existing class. A visual regression test will detect the CSS change but will not flag a structural change that has no visible effect. The two approaches are complementary rather than interchangeable.`})]})}export{o as default};